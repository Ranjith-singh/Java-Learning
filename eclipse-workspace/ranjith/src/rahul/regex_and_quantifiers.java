package rahul;

public class regex_and_quantifiers {

	/* the regex : are ex : abc,[]=character class,^ and $=starting with and ending with respectively
	 * .=any character,[^]=except the characters in the character class
	 * Quantifiers : =,+,?,{X}=it says that the previous regex should occur x no of times
	 * {X,Y}=occurs b/w x and y times,*?=it tries to find the smallest match.this makes the regex stop at the
	 * first match
	 */
}
